temperatures=[10,-20,-289,100]


def celsius_converter(c):
    with open("C:\Python27\example3.txt", 'a') as file:
        rate = 1.8
        suf = 32

        if c > -273.15:
            f = c * rate + suf
            file.write(str(f)+"\n")

for c in temperatures:
    celsius_converter(c)
